focus_tree[id = china_nationalist_focus]:

  focus[id = CHI_three_principles_of_the_people]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_welfare]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_refugee_relief_agency]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_land_tax_reform]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_new_life_movement]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_unemployment_assistance]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_free_hospitals]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_rural_schooling]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_democracy]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_constitutional_reform]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_republicanism]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_inter_party_coordination_council]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_judicial_yuan]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_control_yuan]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_examination_yuan]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_executive_yuan]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_legislative_yuan]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_nationalism]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_foreign_threats]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_prioritize_the_interior]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_united_front]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_pick_a_fight_with_japan]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_anti_communism]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_subjugate_the_warlords]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_war_of_resistance]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_industrial_evacuations]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_war_of_national_liberation]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_scorched_earth_tactics]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_war_of_anti_imperialism]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_forced_conscription]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_dare_to_die_corps]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_military_affairs_commission]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_fortify_shanghai]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_the_chinese_hindenburg_line]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_army_reform]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_bureau_of_investigation_and_statistics]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_60_divisions_plan]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_whampoa_military_academy]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_unified_industrial_planning]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_expand_the_academica_sinica]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_chemical_research_institute]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_rural_reconstruction_movement]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_mining_commission]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_taiyuan_arsenal]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_develop_the_hanyan_arsenal]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_financial_policy]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_price_controls]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_grain_tax]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_reform_the_national_bank]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)

  focus[id = CHI_forced_loans]:
    ai_will_do:
      +modifier:
        is_sandbox_mode_on()
        base(1.0)
