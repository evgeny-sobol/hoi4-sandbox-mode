SOV_sandbox_strategy_plan_common:
  allowed:
    original_tag(SOV)
  enable:
    is_sandbox_mode_on(yes)
  abort_when_not_enabled()

  weight::factor(1)

SOV_sandbox_strategy_plan_the_centre:
  allowed:
    original_tag(SOV)
  enable:
    has_completed_focus(SOV_the_centre)
    is_sandbox_mode_on(yes)
  abort_when_not_enabled()

  weight::factor(1)

SOV_sandbox_strategy_plan_the_left_opposition:
  allowed:
    original_tag(SOV)
  enable:
    has_completed_focus(SOV_the_left_opposition)
    is_sandbox_mode_on(yes)
  abort_when_not_enabled()

  weight::factor(1)

SOV_sandbox_strategy_plan_the_right_opposition:
  allowed:
    original_tag(SOV)
  enable:
    has_completed_focus(SOV_the_right_opposition)
    is_sandbox_mode_on(yes)
  abort_when_not_enabled()

  weight::factor(1)
